[project]
name = "mcp-tutorial"
version = "0.1.0"
description = "Learn MCP by building a calculator and a weather server, then host them in your own AI app"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=2.0",
    "httpx2>=2.0",
    "streamlit>=1.40",
    "groq>=0.12",
]

[tool.uv]
package = false
